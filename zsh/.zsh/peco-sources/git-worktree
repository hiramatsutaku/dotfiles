function peco-git-worktree() {
  local selected
  selected=$(git wt | tail -n +2 | peco --prompt="worktree> " | awk '{print $(NF-1)}')
  if [ -n "$selected" ]; then
    BUFFER="git wt ${selected}"
    zle accept-line
  fi
  zle clear-screen
}
zle -N peco-git-worktree
